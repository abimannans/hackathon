name: Build and Push Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: ./patient-service # Assumes workflow is at repo root
        file: ./patient-service/Dockerfile
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/patient-service:latest

      # Note: The next step would be to deploy to Kubernetes.
      # This typically involves configuring kubectl with cluster credentials
      # and running `kubectl apply -f k8s/`. This is highly environment-specific.
      # - name: Deploy to Kubernetes
      #   run: |
      #     # kubectl configuration and apply commands go here